# Python
venv/
__pycache__/
*.pyc

# 前端
web/node_modules/
# web/dist 提交进仓库：保证 git clone 后免构建直接可用

# 本地密钥与运行状态
server/settings.json
server/export_state.json

# 系统文件
.DS_Store

# 视频文件（体量大，不入库；clone 后自行放入 videos/ 目录）
videos/
